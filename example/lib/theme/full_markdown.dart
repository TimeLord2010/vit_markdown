import 'package:flutter/material.dart';

class FullMarkdown extends StatelessWidget {
  const FullMarkdown({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
